{
  "name": "property-visit-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "export NODE_ENV=production && node ./src/index",
    "dev": "export DEBUG=server NODE_ENV=development && node --env-file=.env --watch ./src/index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "debug": "^4.3.6",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "morgan": "^1.10.0"
  }
}
